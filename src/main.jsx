import React from "react";
import ReactDOM from "react-dom/client";
import TapAppPop from "../tap-app-pop.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TapAppPop />
  </React.StrictMode>
);
